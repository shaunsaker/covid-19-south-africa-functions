{
  "name": "covid-19-south-africa-functions",
  "version": "1.0.0",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "firebase serve --only functions",
    "predeploy": "yarn run build",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "eslint": "^6.7.1",
    "firebase-functions-test": "^0.1.6",
    "typescript": "^3.2.2",
    "@types/node": "10"
  },
  "private": true
}
