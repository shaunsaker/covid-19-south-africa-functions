{
  "name": "covid-19-south-africa-functions",
  "version": "1.0.0",
  "engines": {
    "node": "10"
  },
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "yarn run build:watch && firebase serve --only functions",
    "dev": "ENV=dev concurrently \"yarn run build:watch\" \"sleep 2 && firebase serve --only functions\"",
    "predeploy": "yarn run build",
    "deploy": "firebase deploy --only functions",
    "deploy:dev": "firebase use development && yarn run deploy",
    "deploy:prod": "firebase use default && yarn run deploy"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^8.6.0",
    "firebase-functions": "^3.3.0"
  },
  "devDependencies": {
    "concurrently": "^5.0.0",
    "eslint": "^6.7.1",
    "firebase-functions-test": "^0.1.6",
    "typescript": "^3.2.2",
    "@types/node": "10"
  },
  "private": true
}
